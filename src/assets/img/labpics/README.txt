Lab photos

Drop image files here (.jpg, .jpeg, .png, .webp, or .gif) and rebuild the
site. They automatically appear, in rotation, in the sliding photo album on
the Join the Lab page - no other changes needed.

Files are sorted by filename, so prefix them (01-, 02-, ...) if you want a
specific order. Otherwise alphabetical order is fine.
