# Downloadable files

Drop PDFs here and they are served at `/files/<name>.pdf`.

Two files are already linked from the site and need to be added:

- `Stroud_CV.pdf` — linked from the footer and the Lab Members page
- `Stroud_Lab_Handbook.pdf` — linked from the Join the Lab page

Until you add them, those two links will 404. Everything else works.
